<h1>編集画面</h1>

<%= form_for(@user) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    
    <%= f.label :email %>
    <%= f.text_field :email %>

    <%= f.submit "Update my account", class: "btn btn-large btn-primary" %>


<% end %>